docker run
docker pull image-name
docker ps
docker images
docker run -name container-name image-name
docker run -it -name container-name image-name
docker run -d -name container-name image-name
docker run container /bin/bash
docker images ls
docker history images-name
docker run -e MYVAR="My Environment value"
docker volume create name-of-the-volume
docker run --name volcontainer -v name-of-the-volume:/data -d alpine sh -c "echo 'Hello World' > /data/hello.txt"
docker run --rm -v name-of-the-volume:/data alpine cat /data/hello.txt
docker rm -f volcontainer
docker rm -f volcontainer
##Docker Networking
docker run -d --name container1 alpine sleep 1000
docker run -d --name container2 alpine sleep 1000
docker inspect container1
docker inspect container2
docker inspect container1 | grep IPAddress
docker inspect container2 | grep IPAddress
docker exec container1 apk add --no-cache iputils
docker exec container2 apk add --no-cache iputils
docker exec container1 ping -c 3 container2_IP
docker rm -f container1 container2
docker network create mynet
docker run -d --name container1 --network mynet nginx
docker run -d --name container2 --network mynet nginx

