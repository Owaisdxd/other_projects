sudo apt update
sudo apt install awscli
aws configure
wget -O - https://apt.releases.hashicorp.com/gpg | sudo gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(grep -oP '(?<=UBUNTU_CODENAME=).*' /etc/os-release || lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
deb [arch=amd64 signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com resolute main
sudo apt update && sudo apt install vagrant
vagrant plugin install vagrant-aws
mkdir /home/$USER/vagrant-multi
cd /home/$USER/vagrant-multi
vagrant init
#IF FAILED THE INITIALIZAION
#Issue Most likely dependency
# 1. Make sure Ruby + build tools are available
sudo apt update
sudo apt install -y ruby ruby-dev rubygems build-essential

# 2. Get the vagrant-aws source
cd ~
git clone --depth 1 https://github.com/mitchellh/vagrant-aws.git
cd vagrant-aws
sed -i 's/s.add_runtime_dependency "fog", "~> 1.22"/s.add_runtime_dependency "fog-aws", ">= 1.3"/' vagrant-aws.gemspec
sed -i 's/require "fog"/require "fog\/aws"/' lib/vagrant-aws/action/connect_aws.rb
grep 'fog' vagrant-aws.gemspec
grep 'require "fog' lib/vagrant-aws/action/connect_aws.rb
##You should see fog-aws in the gemspec line and require "fog/aws" in the other.
gem build vagrant-aws.gemspec
##Should see something like this
#Successfully built RubyGem
#  Name: vagrant-aws
#  Version: 0.7.2
#  File: vagrant-aws-0.7.2.gem
vagrant plugin uninstall vagrant-aws
vagrant plugin install ~/vagrant-aws/vagrant-aws-0.7.2.gem
vagrant plugin list
